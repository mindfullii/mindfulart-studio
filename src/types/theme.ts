export type Color = {
  primary: string;
  primaryLight: string;
  textPrimary: string;
  textSecondary: string;
  bgPrimary: string;
  bgSecondary: string;
  border: string;
};

export type FontSize = {
  xs: string;
  sm: string;
  base: string;
  lg: string;
  xl: string;
  '2xl': string;
  '3xl': string;
};

export type FontWeight = {
  normal: number;
  medium: number;
  semibold: number;
  bold: number;
}; 